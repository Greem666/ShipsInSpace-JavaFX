package shipsinspace.view.interfaces;

import javafx.scene.layout.Region;

public interface BottomMenuElements {
    /**
     * Interface for all elements of the bottom panel to follow.
     */
    Region generateElement(double iconSize);
}
